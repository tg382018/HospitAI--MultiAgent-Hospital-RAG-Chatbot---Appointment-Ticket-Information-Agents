VITE_API_BASE_URL=
# Tenant slug sent automatically with every login / register request
VITE_TENANT_SLUG=demo-hospital
# Admin kayıt anahtarı backend/.env içindeki ADMIN_REGISTRATION_KEY ile aynı olmalıdır.
# Hospital display name shown in the admin header
VITE_HOSPITAL_NAME=XYZ Hospital
